# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Test + tooling caches
.pytest_cache/
.coverage
htmlcov/

# Generated codeplugs (outputs, not sources).
# Catch-all: parsed channel data can carry operator names and contact details,
# so no CSV is tracked regardless of what -o named it. Force with `git add -f`
# if a CSV ever genuinely belongs in the repo.
*.csv

# Source forms are real ARES documents; kept local, not published
samples/
